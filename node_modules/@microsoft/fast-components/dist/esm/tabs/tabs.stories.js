import { STORY_RENDERED } from "@storybook/core-events";
import addons from "@storybook/addons";
import Examples from "./fixtures/base.html";
function addItem() {
    const tabsElement = document.getElementById("addTabsExample");
    if ((tabsElement === null || tabsElement === void 0 ? void 0 : tabsElement.children) !== undefined) {
        const tab = document.createElement("fast-tab");
        const tabPanel = document.createElement("fast-tab-panel");
        tab.textContent = "Added tab";
        tabPanel.textContent = "Added panel";
        tabsElement === null || tabsElement === void 0 ? void 0 : tabsElement.appendChild(tab);
        tabsElement.insertBefore(tabPanel, tab);
    }
}
addons.getChannel().addListener(STORY_RENDERED, (name) => {
    if (name.toLowerCase() === "tabs--tabs") {
        const button = document.getElementById("add-button");
        button.addEventListener("click", () => addItem());
    }
});
export default {
    title: "Tabs",
};
export const Tabs = () => Examples;
